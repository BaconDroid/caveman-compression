version: '3.8'

services:
  caveman-compression:
    build:
      context: .
      args:
        LANGUAGES: en,fr
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    environment:
      - PORT=${PORT:-3000}
    restart: unless-stopped
