version: '3.8'

services:
  caveman-compression:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - LANGUAGES=en,fr
    restart: unless-stopped
    volumes:
      - ./models:/root/.cache  # Cache models between restarts
