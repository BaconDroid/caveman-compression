version: '3.8'

services:
  caveman-compression:
    build: .
    ports:
      - "3000:3000"
    environment:
      - LANGUAGES=en,fr
      - PORT=3000
    restart: unless-stopped
    volumes:
      - models:/app/models

volumes:
  models:
